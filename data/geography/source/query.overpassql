[out:json][timeout:120];
// Astana's six district relations and its administrative city boundary.
// Equivalent discovery/export query. Saved inputs were fetched from the OSM API
// because Overpass endpoints timed out; see manifest.json for exact full.json URLs.
relation(id:3479876,3482819,3486954,8593081,20593940,19733918,3087155);
(._; >>;);
out meta;
